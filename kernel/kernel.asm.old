[org 0x1000]

[bits 32]

kernel: jmp k_main

%include "print_string_pm.asm"

msg: db "a kernel in asm, holy hell!", 0

k_main:
    mov esi, msg
    call print_string_pm

.k_mainloop:
    jmp $

    jmp .k_mainloop

times 512 - ($ - $$) db 0
